# 地磁在 EKF 里的可观测性：怎么把"绕磁力线的旋转无观测"写成结构性质

问题：如何设计 EKF 数学，才能**显式**表明"绕磁北矢量的旋转无有效观测"——就像重力观测天然不能修正偏航？

---

## 1. 先把两个传感器写成同构的"方向观测"

设体坐标系下的姿态估计 `R̂`（体→世界），世界系参考矢量 `v̂_n`（重力 `ê_z` 或磁场 `b̂_n`），
体坐标系下的测量单位矢量 `m̂_b`（比力方向 / 已标定磁场方向）。

预测的体坐标系方向：`v̂_b(q) = R̂ᵀ v̂_n`。
用**体坐标系扰动** `R_true = R̂·Exp(δθ)`，一阶展开：

```
v̂_b,true = Exp(−δθ)·v̂_b ≈ v̂_b − δθ × v̂_b = v̂_b + [v̂_b]× δθ
⇒ ∂v̂_b/∂δθ = [v̂_b]×            （[·]× 为反对称阵）
```

**2 维残差**（去掉沿参考方向的不可观测分量，这就是"方向观测"的标准写法）：

```
r = (I − v̂_b v̂_bᵀ)(m̂_b − v̂_b)
H = ∂r/∂δθ = (I − v̂_b v̂_bᵀ)·[v̂_b]×          (3×3, rank = 2)
```

**结构零空间（本文的核心结论）**：

```
H · v̂_b = (I − v̂_b v̂_bᵀ)·([v̂_b]× v̂_b) = (I − v̂_b v̂_bᵀ)·0 = 0      ← 精确成立，不是近似
```

即 `N(H) = span(v̂_b)`，**且与测量值、与标定好坏无关**。对照两种情况：

| 传感器 | 参考矢量 `v̂_n` | Jacobian | 零空间（不可观测方向） | 物理说法 |
|---|---|---|---|---|
| 加速度计 | `ê_z`（重力） | `H_g = (I − ĝ_b ĝ_bᵀ)[ĝ_b]×` | `span(ĝ_b)` | **绕重力（=偏航）不可观测** ⇒ 重力不该、也不能修偏航 |
| 地磁 | `b̂_n`（磁力线） | `H_m = (I − b̂_b b̂_bᵀ)[b̂_b]×` | `span(b̂_b)` | **绕磁力线不可观测**（不是绕世界竖直！） |

注意两处容易混淆的地方：

1. 地磁盲的是**磁力线** `b̂_b`（磁北再按磁倾角下倾 I≈64.3°），**不是**水平磁北方向，也不是世界竖直。
2. 重力盲的是 `ĝ_b`。两者**不平行**（夹角 = 90°+I），所以

```
[H_g ; H_m]  满秩  ⟺  ĝ_b ∦ b̂_b，其条件数由 |ĝ_b × b̂_b| = cos(I) ≈ 0.43 决定
```

**偏航之所以可观测，恰恰是靠"重力看不见的那一维由地磁补上"**；任何单个传感器都缺一维。
本地 dip=64.3° ⇒ `cos(I)=0.43`，也就是偏航信息只有水平分量的 43%，
噪声/误差放大 `1/cos(I)=2.30`；而**倾角误差混进偏航**的斜率是

```
∂ψ/∂δθ_east = tan(I) = 2.08        ← 与现有常数 V5F_EKF_DIP_TAN = 2.08 一致
∂ψ/∂δθ_yaw  = 1
```

这解释了 `proc_ekf.c` 里 R 的写法 `R² = R0² + (dip_tan·σ_tilt)²`：倾角不确定度按 `tan(I)` 混入偏航新息。

---

## 2. 现有实现处于哪一档（对照）

- **重力侧已经是"显式"写法**：`ekf_m6`（`proc_ekf.c` L476-507）
  `p = Rᵀê_z`、`r = (I − ppᵀ)(f̂ − p)`、`H = (I − ppᵀ)M` ⇒ `H·p = 0`。
  绕重力不可观测是**结构**保证的，"重力不修偏航"不需要靠门控去压。✅
- **地磁侧是标量形式**：`ekf_m7_mag`（L638+）只有 1 维残差 `r[0]`（
  用 dip/decl 固定参考 `b0=(ci·sin δ, ci·cos δ, −dip_tan·ci)` + 陀螺把磁样本推到当前时刻）。
  标量观测的零空间是**平面（2 维）**，其中含 `b̂_b`，但"绕 b̂ 不可观测"这句话**说不清楚**——
  这就是"要显式化"的动机。并且它的 R 用一个各向同性的放大项把 `tan(I)·σ_tilt` 折算进去，
  属于"承认耦合"而不是"结构解耦"。

---

## 3. 三种可选数学形式（按"显式程度/改动量"排）

### A. rank-2 对称式（最显式，和重力完全同构）
```
r = (I − b̂_b b̂_bᵀ)(m̂_b − b̂_b)          b̂_b = R̂ᵀ b̂_n
H = (I − b̂_b b̂_bᵀ)[b̂_b]×                 H·b̂_b ≡ 0
```
- 优点：零空间可证、与重力同构，代码结构可以复用同一套 `(I − ppᵀ)` 机制；
- 代价：地磁同时修正 roll/pitch（与重力**信息重复**）。标定不好时两者互相拉扯——
  平面标定下不建议这样开。

### B. rank-1 互补式（推荐：分工显式，且只用平面标定也稳）
让地磁**只做重力做不了的那一维**：
```
Π_g = ĝ_b ĝ_bᵀ                      （体坐标系"竖直"方向的投影，即偏航方向）
H_m^yaw = Π_g · H_m                  rank = 1
H_g     = (I − ĝ_b ĝ_bᵀ)[ĝ_b]×       rank = 2
rank(H_g) + rank(H_m^yaw) = 3  ⇒ 显式分工：roll/pitch 归重力，yaw 归地磁
```
- `H_m^yaw·b̂_b = Π_g·(H_m b̂_b) = 0` 仍成立 ⇒ "绕磁力线不可观测"照样是结构性质；
- 地磁不再触碰 roll/pitch ⇒ 与"只做过平面 360 标定、竖直信息不可信"的现状**一致**；
- 这是把"重力禁止修偏航"对称化后的自然结果：**重力只在 ⊥ĝ 平面内做功，地磁只在 ∥ĝ 方向做功**。

### C. 标量偏航 + 显式解耦项（改动最小，等于给现在的 M7 补上解耦）
```
r_yaw = (ψ_meas − ψ_pred) − (∂ψ/∂tilt)·δθ̂_tilt      （Schmidt/consider 式解耦）
∂ψ/∂δθ_east = tan(I) = 2.08,   ∂ψ/∂δθ_yaw = 1
```
- 把"倾角能解释的部分"先扣掉，只把**重力看不见的那一维**交给地磁；
- 必须把**扣掉的量**一并记录（否则等于把耦合藏起来了）。

---

## 4. 怎么在固件/日志里"显示地表明"（可验证，而不是靠嘴说）

1. **结构自检**（和现有 `p_p_zz`/`prop_ok` 自检同风格，逐帧或抽帧算）：
   - `n_b = ‖H_m·b̂_b‖`（应 ≈ 0）、`n_g = ‖H_g·ĝ_b‖`（应 ≈ 0）
   - `s = |ĝ_b × b̂_b|`（≈cos(I)=0.43；决定联合可观测性与条件数）
   - 信息矩阵 `F = Σ Hᵢᵀ Rᵢ⁻¹ Hᵢ` 的 3×3 特征分解：`λ_min` 与其特征向量 `v̂_min`
     —— 只开重力时 `v̂_min = ĝ_b`，只开地磁时 `v̂_min = b̂_b`，两者都开时无零特征值。
   - 每次更新的"零空间泄漏" `‖(δθ_applied·v̂_min) v̂_min‖`（应 ≈0）。
2. **离线验证（不需要改固件）**：162 通道调试帧里已经有 `mag.f`（体坐标系已标定磁场 ⇒ `b̂_b` 的测量）、
   `accel_g`（⇒ `ĝ_b` 的测量）、`ekf.q`、`mag_r_deg`、`mag_dq{x,y,z}`、`nis[4]`。
   用这些就能重建 `H_m`/`H_g`，数值验证上面几条恒等式，并回归出
   "倾角误差 → 偏航新息"的斜率是否 = `tan(I) = 2.08`。
3. 若要把 `λ_min/v̂_min` 直接画出来：最省的做法是把 `|ĝ×b̂|` 与 `λ_min` 加进**抽帧后**的调试帧，
   不要在 8 kHz 里做特征分解（放到 1/24 抽帧那一帧算即可）。

---

## 5. 对"下一步地磁 360 标定"的直接含义

- 平面 360 标定只约束了水平面：A 的竖直行/列与 C 的竖直项（以及出平面的交叉轴项）没被约束住。
  而上面的数学说明：**竖直/出平面误差会按 `sin(倾角)`/`tan(I)` 的几何混进航向通道**，
  表现为"牵引量随姿态变化"——这正是"地磁牵引与姿态误差放大"的耦合来源之一。
- 3D 椭球标定（A 的 9 项 + C 的 3 项，可对称化）把这个耦合通道压掉，剩下的
  `∂ψ/∂tilt = tan(I)` 是**物理**耦合，只能靠 R / 解耦项处理。
- 标定后的验收判据（都用现有/计划中的日志列）：
  1. `|‖A·raw + C‖ − 1|` 分布（`mag_norm` 列）：目标 <1~2%；
  2. `H_m b̂ ≈ 0` 的结构残差（上面第 4 节）；
  3. `mag_cmp_thm`（实测航向）− `mag_cmp_thp`（预测航向）随 roll/pitch 的回归斜率应无可辨识趋势；
  4. 偏航残差 vs 倾角的回归斜率应 ≈ `tan(I)=2.08`（标定差会让它偏离且随姿态系统性漂）；
  5. 全姿态 360 闭合：绕任意轴转 360° 回原点，偏航残差与 roll/pitch 的相关性 ≈ 0。

---

## 6. 一句话总结

把地磁写成**方向观测**（`r=(I−b̂b̂ᵀ)(…)`、`H=(I−b̂b̂ᵀ)[b̂]×`）时，
"绕磁力线的旋转无观测"就是 `H·b̂ ≡ 0` 这条**结构恒等式**，和"重力不能修偏航"（`H_g·ĝ ≡ 0`）完全同构；
工程上再补两份东西——(a) `H_m` 只保留 `Π_g = ĝĝᵀ` 这一维（分工显式），
(b) 把 `‖H·v̂‖`、`|ĝ×b̂|`、信息矩阵 `λ_min/v̂_min` 打进日志（可验证）。

---

## 7. 实现记录：VER=103 已按方案 A 落地（真牵引）

`V5F_EKF_MAG_MODE = 1u`（0u 为旧标量牵引，回退用）。代码在 `proc_ekf.c: ekf_m7_mag()`。

**数学形式（实际写进固件的那份）**

```
b̂_b = R(q̂)ᵀ · b̂_n                    预测的机体系磁场方向
r    = P_⊥ m̂ = m̂ − (m̂·b̂_b) b̂_b         2 维残差（⊥b̂ 平面内；不做姿态投影）
e1   = normalize(ẑ × b̂_b),  e2 = b̂_b × e1     ⊥b̂_b 的正交基
H    = −c1 · e_iᵀ [b̂_b]× R̂ᵀ            i=1,2；c1 = m̂·b̂_b
更新  ekf_update(R=σ²I₂, m=2, r2, …, inj_mask=0x01C0)   ← 作用在旋转矢量 dx[6..8]
```

三个容易踩的坑（都由 `tools/ekf_session/check_v103_magvec.py` 的有限差分检查抓出来并修正）：

1. **扰动约定**：本固件的姿态扰动是**世界系**的（`ekf_inject`: `q ← dq ⊗ q`；M6 的 `Rᵀ[up]×` 同理），
   所以 H 必须带 `R̂ᵀ`，结构零空间方向是**世界系磁场方向** `b̂_n`（`H·b̂_n ≡ 0`）。
   按体坐标系写成 `−e_iᵀ[b̂]×`（零空间 `b̂_b`）会差一个 `R̂ᵀ`——方向全错。
2. **残差写法**：`P_⊥m̂ = m̂ − (m̂·b̂)b̂`；写成 `(m̂−b̂) − (m̂·b̂)b̂` 会多减一个 `b̂`。
   虽然再投影到 `⊥b̂` 后数值被消掉，但 H 就不再是它的 Jacobian（差分差 7.5e-4）。
3. **H 里的 `c1`**：`c1 = m̂·b̂ ≈ 1` 也要乘进去，否则同样破坏自洽（≈ 1% 残差时差 1.5e-4）。

**参数与门控（VER=103 的取舍）**

- `R = σ² I₂`，`σ = V5F_EKF_MAG_VEC_SIG_DEG = 0.9°` —— 取自本机实测方向误差（p50 0.5°/p90 1.3°，
  见 `mag360_recording_protocol.md` §6）；**不再**用 `R² = R0² + (tan(dip)·σ_tilt)²` 的人为放大。
- `V5F_EKF_MAG_VEC_K_MAX = 0.10`：单次更新步长 = `K·|r|` 的硬上限（防磁样本把姿态猛拽）。
- **取消"运动中禁磁"**：删掉原 `V5F_EKF_MAG_GT_HOLD_S` 那条 hold 与 `s_mag_tilt_bad_s`。
  那条是为"姿态误差 → 重力法平面投影误差 → 地磁误差"设的，是**投影式**观测的产物；
  真牵引在机体系做差没有这个耦合。合法性只看**模**：
  外门 `gate->ekf_mag_yaw = (|mag_norm − 1| < V5F_MAG_ERR_LIM)`，另有残差上限 `R_MAX` 与
  "同一磁样本不重复更新"（磁 ~166–190 Hz vs 帧 ~335 Hz）。

**验证（本机无 C 编译器，改用数学等价 + C 结构检查，全部通过）**

| 检查 | 结果 |
|---|---|
| `e1,e2` 正交且 ⊥ `b̂_b` | 3.3e-16 |
| `\|H·b̂_n\|`（结构零空间 / 绕世界磁场轴无观测） | 3.0e-16 |
| `H` vs 中心差分 Jacobian（世界系扰动、冻结基） | 7.7e-10 |
| 姿态绕 `b̂_n` 旋转 → 残差不变 | 6.2e-10 /rad |
| 完美量测下 `\|r2v\|` | 9.2e-17 |
| 去注释后括号平衡 / `#if-#endif` 配对 / 无禁磁残留 / tune 常量 | 全部 OK |

**上机后要看的列**（调试帧）：`ekf_mag_used`(120) 应在**运动中**也接近满（旧版运动期被 hold 掉）、
`ekf_mag_hold`(151) 恒 0、`ekf_mag_r_deg`(119) 与 `ekf_mag_dq*`(135–137) 给出步长、
`nis[4]`(112) 的分布、`mag_norm`(45) 仍是唯一的合法性门。
A/B 只需把 `V5F_EKF_MAG_MODE` 在 1u/0u 之间切一次重编。

### 7.1 首轮上机检查发现的符号错误（已修，记录在案）

`R:\imu_20260921_024439.bin`（VER=103，30 s）证明新模式**确实生效**：
`fw_tag=6791687`、`mag_rs` 恒 **1.00**、`mag_dqx/dqy` 非零 **48%/48%**（旧版只有 `dqz`）、`mag_hold` 恒 0。
但同时暴露了一个**致命符号错**：

| 列 | 新录像（错） | VER=102 基线（旧标量） |
|---|---|---|
| `mag_r_deg` | **p50 54.2° / max 57.296°** | 0.448° |
| `nis[4]` | **p50 2907 / max 4053** | 0.00 |
| `mag_rej` | **250（饱和）** | 0 |

诊断链：`57.296° = 1.0 rad` 说明上报的是 `|r| = sinθ` 而不是夹角；`sinθ→1` ⟺ **θ→90°**，
即实测场与预测场正交 ⇒ EKF 偏航被推到与地磁正交。原因是 **H 的符号**：
`ekf_update` 是标准形式（`H = dh/dx`、`r = z − h`、`dx = K r`；旧标量模式正是
`r = thm − thp`、`H=+1`），而我按"残差的导数"写成了 `H = −e_iᵀ[b̂]×Rᵀ` ⇒ 每次更新把姿态
**推离**量测（正反馈），偏航一路跑到 90°、NIS 爆到 4000、χ² 软缩放把步长压到 0.02°/次。

修正（三处，全部已在 `check_v103_magvec.py` 里加了对应检查）：

1. **H 取正号**：`H = +e_iᵀ[b̂_b]×R̂ᵀ`（= `dh/dx`，`h = e_i·b̂_b`）。新增自检 A7：
   姿态有小误差 `d` 时 `r·(H d) < 0`（更新必须朝收敛方向）。
2. **残差直接写 `r2v_i = e_i·m̂`**（因 `e_i ⊥ b̂_b`，与 `e_i·(m̂ − c1 b̂)` 完全同值），
   这样 `H = dh/dx` 与残差一一对应；旧写法多出的 `c1` 形式导数会让差分差 7.5e-4。
3. **`mag_r_deg` 改报夹角** `θ = atan2(|r2v|, m̂·b̂_b)`（旧写法把 `sinθ` 当角度，
   90° 会显示成 57.3°）。

修正后检查全绿：`H = dh/dx` 差 7.8e-10、`H·b̂_n` 3.0e-16、绕 `b̂_n` 残差不变 7.3e-10、
`θ` 上报一致 1.4e-11、符号自检通过。**下一轮录像应看到**：`mag_r_deg` p50 落在
0.5–1.5°、`nis[4]` 量级 1–10、`mag_rej` 回到 0、`mag_used` ≈ 磁/帧速率比（~50%）、
`mag_rs`=1.00、`mag_hold`=0。

### 7.2 VER=105：旧标量模式**已彻底删除**（不留条件编译）

问过一句"为何要保留旧模式"——不保留。VER=105 起：

- `proc_ekf.c` 里 4 个 `#if (V5F_EKF_MAG_MODE == 0u)` 块整块删除（R/r 声明、旧 R 模型、
  旧标量新息、模式 0 更新分支），2 个 `!= 0u` 条件行去掉（内容保留）。
  M7 里现在**没有任何条件编译**，也没有 `MODE` 开关。
- 随之删除的孤儿常量：`V5F_EKF_MAG_MODE / MAG_K_MAX / MAG_R_DEG / MAG_TILT_CAP_DEG /
  MAG_RSCALE_MAX / MAG_DEAD_DEG / MAG_GT_HOLD_S / MAG_YAW_R_DEG / EKF_MAG_SIG_RAD`，
  以及静态量 `s_tilt_sig_deg`（只服务于旧 R 模型）。
- **一处真实依赖**必须一起改：`V5F_EKF_YAW_P_MIN` 原本是 `(V5F_EKF_MAG_SIG_RAD)^2`
  （0.5 度旧量测噪声），删掉那个宏就编不过；现改真牵引的 sigma
  `((V5F_EKF_MAG_VEC_SIG_DEG*DEG2RAD)^2)`。
- gcc 那几条 unused 警告（`R[1]`/`r[1]`/`sig2`/`pr2`/`s_tilt_sig_deg`）全部消失。
- `s_mag_hold` 保留：它是对外列 151，值恒 0（禁磁已去，留列不动帧格式）。
- 对外列一个没动：`mag_r_deg`(119) = 夹角 θ（度）、`mag_rx/ry`(128/129) = ⊥b̂ 两维新息（度）、
  `mag_rs`(149) 恒 1.00、`mag_cmp_thm/thp`(144/145) 仍是实测/预测航向诊断。
- 验证脚本合并为 `tools/ekf_session/check_magvec.py`（A1–A7 数学 + C 结构，含"旧模式零残留"）。
- 想回看旧行为：git 历史里的 VER=102/103 提交即可，不需要在源码里留开关。

### 7.3 VER=106：磁倾角常量取错了（真牵引把倾斜一起拉，于是暴露出来）

**一个与姿态无关的判据**（只用"标定后的磁场方向 vs 加计重力方向"，静态帧 |a|=1g±1%、|ω|<3 dps）：

| 录像 | 静态样本 | angle(b̂, up) 中位 | **实测 dip** |
|---|---|---|---|
| `025243`（VER=105） | 3078 | 149.04° | **59.04°** |
| `023242`（VER=102） | 6643 | 149.98° | **59.98°** |
| `022718`（标定来源） | 6397 | 149.73° | **59.73°** |

三段独立一致、**按偏航四档分档差异 ≤0.15°**（⇒ 不是规范自由度、不是标定失准），
加计残余零偏 ≤5 mg 只带入 ≤0.3°。取 **I = 59.6° ⇒ tan I = 1.70**。

而固件原值 `V5F_EKF_DIP_TAN = 2.08` ⇒ I = 64.26°（对应磁纬 ≈46.1°N），
59.6° 对应磁纬 ≈40.4°N（`tan I = 2 tan λ`）——**原常量像是取错了纬度**（差 ≈4.7°）。

A/B 证据（同协议两段录像）：真牵引上线后磁把**倾斜也一起拉**，于是模型误差直接拽倾斜：

| 指标 | VER=102（只修偏航） | VER=105（真牵引 2 维） |
|---|---|---|
| 重力 NIS `nis[3]` p50 / p90 | **0.11 / 17.3** | **43.8 / 166.4** |
| M6 残差（倾角门开时）p50 / p90 | **0.17° / 0.42°** | **3.87° / 5.23°** |
| `sigma_tilt` p50 | 13.46° | 3.77° |

被拽偏的量（3.9~5.2°）与模型误差（4.7°）同量级 ⇒ 诊断成立。**修正**：
`V5F_EKF_DIP_TAN = 1.70f`，VER → 106。修好后应看到 `nis[3]` 回到 O(1)、
M6 残差回到 <0.5°、而磁新息仍保持 ~0.5°（即"磁与重力不再打架，两边都满意"）。

备选（若将来测出单点 dip 不足以描述环境畸变）：把量测限制在重力盲方向
（rank-1，`H ← Π_g H`，`Π_g = ĝ_b ĝ_bᵀ`）——即数学文档里的方案 B，代价是丢掉
"不受线加速度影响"的磁倾斜信息，好处是完全不依赖 dip 常量。

### 7.4 VER=107：竖直基准改为重力矢量 · 倾斜权按**置信度**降

**实测：表观磁倾角随环境/位置变，不随姿态变。**

| 录像 | 静态帧 | 表观 dip | `\|b̂\|` 中位 |
|---|---|---|---|
| 022718（标定来源） | 6397 | 59.73° | 1.000–1.005 |
| 023242 | 6643 | 59.98° | 0.977–0.995 |
| 025243（VER=105） | 3078 | 59.04° | 1.010 |
| **025957（VER=106）** | 3339 | **57.70°** | **1.016** |

原始磁场 LSB 几乎一样（x 82~89、y 20~26、z −151~−153），但方向差 2~3°、幅度差 1.6%
⇒ 是**近旁铁磁物/摆放位置**变了（不是姿态、不是规范自由度）。所以：

1. **竖直基准一律取重力矢量**（加计方向；夹具台不平也不影响），不再用 ẑ/水平假设。
2. **单点 dip 常量最多可信到 ±1.5~3°** ⇒ 不能让磁的倾斜信息以固定权重进姿态；
   改为**按置信度降权**：实测倾角 `dip_m = angle(m̂, gn) − 90`（重力基准）与模型
   `atan(DIP_TAN)` 之差 `ddip`，置信度 `c = 1/(1+(ddip/REF)²)`（下限 `CONF_MIN`），
   倾斜行 `σ_tilt = σ_yaw / c`：
   - 干净环境 `ddip→0` ⇒ `c→1` ⇒ 磁的倾斜信息**按 σ_yaw 正常参与**；
   - 本台面 `ddip≈2°` ⇒ `c≈0.2` ⇒ 倾斜权降 ~20–40 倍（方差），重力重新主导倾斜。
   `s_mag_rs`(149) 改为上报**倾斜行的 R 放大倍数** `(σ_tilt/σ_yaw)²`（列用途不变）。
3. 面内基的**偏航方向**由重力轴定义：`e1 = normalize(gn × b̂_b)`——它只是
   `(gn, b̂_b)` 平面法向，**与 dip 模型误差无关**（检查脚本 A8 用有限差分证实：
   把模型场在竖直面内转 ±5°，偏航残差与 `e1` 变化 3.6e-16）。

**检查抓到的两个错（都已修）**：
- 固件里 `dip_m` 写成了 `90 − angle` ⇒ 磁场朝下时得 **−57°** ⇒ `ddip≈119°` ⇒
  置信度被永久压到下限、倾斜通道等于关死。应为 `angle − 90`（A9 检查专门盯这个）。
- 检查脚本 A8 最初把"竖直面内旋转"的轴取成了体坐标轴（模型场是世界系向量，
  轴要换算 `R·k_body`）——修好后 A8 从 0.17 降到 3.6e-16。

**上机应看到**（VER=107）：`nis[3]` 回到 O(1)、M6 残差 <0.5°、磁新息仍 ~0.5°、
`mag_rs` 给出当时的倾斜降权倍数（实测：慢/水平阶段 ≈1 全权参与；快/牵引阶段触底，
倾斜被自动压掉），`mag_dq*` 的倾斜分量随之被压。

### 7.5 极限录像验收（VER=107，快/大幅度）：修正作用均匀且正面

数据：`R:\imu_20260921_030649.bin`，30 s，**行程 17217°**、|ω| p90 2128 / max 2763 dps、
**削顶 724 帧（7.2%，仅 X 轴；削顶时 |ω| 中位 2311 dps > ±2000 dps 满量程）**、
`fw_tag` = VER=107、校验和 100%。

判据只用物理不变量"世界系磁场方向应恒定"（`tools/calib/mag_effect.py`）：

| 指标 | EKF（磁牵引） | 旧链 att.q | 倍数 |
|---|---|---|---|
| 世界系场方向散布 p50 / p90 / max | **1.49° / 8.84° / 50.1°** | 47.6° / 81.0° / 123.5° | 32× / 9× |
| 同上 rms | **1.58°** | 36.65° | 23× |
| 最快档（800–1600 dps）p90 | **17.1°** | 106.8° | 6× |

- **均匀性**：`mag_used` 每秒都是 **47–49%**（= 磁 166 Hz / 帧 335 Hz 的刷新上限），
  **全程没有空档**；新息 θ 随 |ω| 单调增大（0.23° → 5.11° 中位），说明快的时段姿态误差更大、
  地磁在持续纠（4819 次更新，|dq| 均值 0.247°、max 4.27°/次）。
- **正面性**：修正是随机/往复为主而非单调纠偏（Σyaw −176.7° vs Σ|yaw| 770.0° ⇒ 相干比 0.229）；
  倾斜通道与重力一致（M6 残差 p50 0.577° / p90 0.777°，`nis[3]` p50 0.45），
  同时把 `sigma_tilt` 从旧模式的 13.46° 压到 **0.38°** ⇒ 磁的倾斜信息是"加信息"而非"打架"。
- **置信度机制按设计动作**：`mag_rs` 在 s0/慢段 ≈1（ddip≈0.5° ⇒ 倾斜全权参与），
  在快段（s2–s9）**触底 2500** ⇒ 牵引让加计不可信时，倾斜通道被自动压掉、只留航向牵引。
- **剩余误差不是地磁**：旧链自身 36.65° rms（已超出纯标度模型；1000 ppm × 17217° ≈ 17° 只是其中一部分）
  + 7.2% 削顶帧（陀螺轨到满量程，地磁无法回天：K 上限 0.10 与 χ² 软缩放限制了权威）。
  地磁工作完之后，EKF 的标度型残差只剩 **+82 ppm / 1.58° rms**。

**动画复现**：python tools/calib/mag_anim.py R:\imu_20260921_030649.bin --fps 10
（四格：3D 姿态+场向量 / 不变量残差 vs 旧链 / 角速率与削顶 / yaw-tilt 修正与 mag_rs；
静态预览图见 docs/img_mag_show_preview.png，t=7.5 s。注意 matplotlib 不能直接写 subst 盘（R:），
先写本地再拷。）

### 7.6 VER=107 录像复查：运动中"牵引被掐住"、停稳后"一次大修正"的定量定位

录像 `R:\imu_20260921_030649.bin`（VER=107，10039 帧 / 30.0 s，dt=2989 µs）。用户观感：
运动中牵引似乎没起作用、残差在攒，停稳后还有一次大修正。逐帧核对后**两件事都成立，但成因不同**。

**工具（都已入库，可复现）**
- `tools/calib/mag_motion_diag.py`  逐秒总览 + 停稳放大 + `--zoom t0 t1` 逐 10/20 ms 细看
- `tools/calib/mag_feedback_check.py`  Δrx 对 rx / dq 的回归（判负反馈还是正反馈）
- `tools/calib/mag_inject_check.py`  反推注入约定（`dq⊗q` vs `q⊗dq`）+ 每次更新是否减小失配角 θ
- `tools/calib/mag_sign_fd.py`  数值微分验证 H = dh/dx 与反馈符号

**(1) 运动期：修正被 K 上限掐住（实测就是绑定约束）**
- 剧烈段 t=3~10 s：|ω| p50 1978 dps、max 2763 dps，**X 轴削顶帧占比最多 41%/s（全段 724 帧，7.2%）**；
  陀螺量程经代码确认为 **±2000 dps / 16.4 LSB/(°/s)**（`SPI_rx.h`、`v5f_tune.h:101`）→ 这段是**硬件削顶**，
  误差注入由它主导，任何滤波器都补不回来。
- 磁更新确实在施加（`mag_used` ≈ 48% = 新鲜度上限，`rej` 只有 ~10/s），但**每次只吃掉创新的 ~10%**：
  实测回归 `dq_yaw / rx = 0.197 ≈ K_MAX(0.10) / cos(dip)(0.51)`，
  即 `V5F_EKF_MAG_VEC_K_MAX = 0.10` 把闭环时间常数钉在 10 次更新 ≈ **61 ms**。
- 结果：残差 rx 停在 **4.17°(p50) / 12.7°(p90)**（静置时是 0.16°），res p90 32.6°。
  ⇒ "运动中牵引没起到有效作用"**属实**，原因是增益上限，不是符号或几何。

**(2) 停稳后的大修正：攒的是 tilt（55°），不是 yaw**
铁证——t=13.90→14.60 期间：|ω| 0~20 dps、加速度机体系 (0.005,0.025,1.000)、磁场机体系
(−0.11,−0.50,−0.855) **全部逐帧不变**，且 `|m|`=1.01~1.03、`dip_m` 59~62° 不变（不是外部磁场跳变），
但 EKF 姿态实际转了 **122°**，转轴 (−0.05,−0.64,0.77) ≈ 世界系水平轴 ⇒ 是**倾斜**在补：

| t (s) | `R^T ẑ_w` 与实测重力夹角 | 说明 |
|---|---|---|
| 13.90 | **55.1°** | 运动结束瞬间 tilt 差 55° |
| 14.20 | 39.9° | 重力通道开始拉 |
| 14.42 | 1.8° | 基本拉回 |
| 14.60 | 7.1° | 过冲后回稳 |

同段固件量：`ekf_grav_nis` 冲到 **1052**、`gate_tilt` 0/1 反复、磁 `nis4` 冲到 18、`th` 冲到 17.5°、
`σ_yaw` 30.6°→6.3° —— 这就是眼睛看到的那次"大修正"。它之所以把 yaw 也拖了 60~100°，
是因为 e2 = b̂×e1 的竖直分量 = cos(dip) ≈ 0.51，倾斜修正天然带 51% 的 yaw 耦合（tan(dip) 耦合的另一面）。

**tilt 为什么能攒到 55°（本段真正可修的缺陷）**
1. 动态中加速度混入离心项，重力通道不可信 → 被 NIS 门关掉（grav_nis 上千）；
2. VER=107 的 `conf` 用 `dip_m = angle(m̂, accel) − 90` 判断磁场可信度 —— **等于用被污染的加速度
   去判磁的诚信度**，于是动态中 ddip 大 → conf 落到下限 0.02 → σ_tilt = 45° → **磁的 tilt 通道也被掐掉**；
3. 于是剧烈段 tilt 既无重力观测也无磁观测，只能靠（削顶的）陀螺推算 → 攒到 55°。

**(3) 已排除的怀疑**
- H 的符号/几何**没错**：`mag_sign_fd.py` 数值微分给出 |H+J| = 4.7e-3 ~ 1.3e-1（H = dh/dx，
  r = z−h 约定下 J = −H 是**正确**关系），且 r·Δr < 0（K 从 0.1 到 1.0 都是负反馈）。
- 注入约定 = **世界系 `dq ⊗ q`**：静止段用发布 q 反推，`dq⊗q` 残差 0.060° vs `q⊗dq` 0.917°。
- 不是外部磁场跳变、不是 mag 采样陈旧（`age_ms` 2~6 ms、`hold` 0）、不是标定 A/C 出错。

**(4) 修法（按收益排序；都是常量/局部改动，不动 H、e1/e2、注入约定、dip 常数、NIS 门）**
1. **`conf` 不许用被污染的加速度算**：只在重力通道可信（|a|≈1 g、重力 NIS 小、`gate_tilt`=1）时更新
   `conf`，否则保持上一次值（或限速回落）。→ 动态中磁的 tilt 通道不再被自掐，55° 攒不起来。
2. **`V5F_EKF_MAG_VEC_K_MAX` 0.10 → 0.3~0.5**：实测它是绑定约束，时间常数 61 ms → ~20 ms，
   剧烈段残差预期从 4.2° 降到 ~1°。
3. **陀螺量程 ±2000 → ±4000 dps**（ICM-42605 支持，16.4→8.2 LSB/(°/s)）：削顶是根因，消掉它比 2 更根本。

### 7.7 修正 §7.6：攒下来的误差在**地磁盲区**里，任何磁权重都修不了

§7.6 有两条结论**错误，此处更正**：

1. ~~"`conf` 用被污染的加速度算，导致动态中磁的 tilt 通道被掐掉"~~ —— **不是本次的原因**。
   `RRv[3] = (SIG/conf)²` 只是把 σ_tilt 放大，通道**从来没有被关掉**，一直在参与牵引；
   而且实测每帧修正量 `dq_yaw/rx = 0.197 = K_MAX(0.10)/cos I(0.507)`，说明 **`MAG_VEC_K_MAX = 0.10`
   才是绑定约束** —— 此时 σ_tilt 取 45° 还是 0.9°，修正量一模一样。用户判断正确：声明"非偏航误差较大"
   并不等于退出牵引。
2. ~~"陀螺量程 ±2000 → ±4000"~~ —— ICM-42605 最高档就是 **±2000 dps**，改不了，撤回该建议。

**真正的原因：那 55° 落在磁力计的盲区里 —— 绕 b̂ 的转动。**
证明（`tools/calib/mag_blind_diag.py`）—— t=13.90 s，器件水平、|a|=1.000 g、|ω|=4~22 dps，
即**最干净的静止参考**：

| 量 | 值 | 含义 |
|---|---|---|
| θ_mag = ∠(m̂_meas, b̂_b(R)) | **0.3°** | 地磁残差（可见的 2 维）几乎为 0 |
| θ_grav = ∠(ĝ_b, R^T ẑ_w) | **55.1°** | 重力方向差 55° |
| ψ ≈ θ_grav / sin∠(m̂,ẑ) = θ_grav/0.507 | **109°** | 绕 b̂ 的真实错误角 |

绕 b̂ 转 ψ 时 b̂ 自身不变 ⇒ 地磁读数完全不变（θ_mag ≡ 0），而重力方向动 ψ·sin∠(m̂,ẑ)。
**这就是磁力计的经典不可观方向**：它只能看见 ⊥b̂ 的 2 维，绕 b̂ 的 1 维只能由重力看见
（重力看不见的是绕 ĝ 的偏航 —— 两者互补，联合才满秩）。所以"地磁为何没修"的答案是：
**它看不见**，不是它没使劲。

**盲区为什么会跑到 109°，以及为什么停稳后 0.46 s 才拉回**

| 事实 | 数值 | 来源 |
|---|---|---|
| 该模式唯一的观测者是重力通道，而这段它全程关闭 | `grav_ok` = 0% | 实测 |
| 关闭是**合法**的：加速度被真加速度污染 | \|a\| p50 **4.0~10.6 g**、峰 **17.6 g**（量程 2026 LSB/g = ±16 g） | 实测 |
| 同时 \|ω\| 远超倾斜门的角速率上限 | `V5F_EKF_TILT_WMAX_DPS = 61.2 dps` | v5f_tune.h |
| 于是盲区只能靠被削顶的陀螺推算 | 真实角速率 >2000 dps、X 轴削顶最多 43%/s | 实测 |
| 重力通道的增益被限死，所以"还款"很慢 | `V5F_EKF_TILT_K_MAX = 0.010` ⇒ 100 次更新 ≈ **0.30 s** | v5f_tune.h |

实测 13.96→14.42 s 把 55° 拉平（0.46 s）与 1%/帧的恢复速度一致。**注意倾斜门的 σ 其实已经按
ω²r/g 软化了**（`eac = wr²·AC_ARM_M/G`，r=0.15 m），61.2 dps 正是 eac=1.0° 的拐点；问题是那里是**硬门**，
拐点之外直接关闭，于是盲区完全没有观测。

**结论（对"纯 IMU 悬停/手感"的含义）**
- 这次 109° 盲区漂移是**极限测试的产物**：真实角速率超过 ±2000 dps 量程、\|\|a\|−1\| 到 10 g。
  这两条在正常飞行里都不成立（\|\|a\|−1\| 几个百分点、角速率多数时间 < 61 dps），
  况且不削顶时盲区的注入速率只有陀螺零偏/标度级（≪1°/s），漂移量是度数级、不是百度级。
- 磁这边唯一还能改的是**可见 2 维的带宽**：`MAG_VEC_K_MAX` 0.10 → 0.3，剧烈段可见残差 2~10° → 约 2°。
  它对盲区**没有任何作用**（数学上不可观）。
- 盲区只能靠重力：若希望在机动中也不放开它，可把 `TILT_WMAX_DPS` 放宽（σ 已有 ω²r/g 软化，
  放宽是平滑降权而不是硬崩）；但超量程削顶期间，该模式本质上就是不可观测的，无融合手段可补。

### 7.8 新录像验收：`imu_20260921_033915.bin`（VER=107）—— 基线通过，且把 §7.7 的结论钉死

`fw_tag = 7053831`（=107<<16 | 162<<8 | 7）与编译 tag 一致，**校验和 100%**，10042 帧 / 30.0 s。
测试动作：持续约 20 s 大幅摇晃（|ω| p50 0.8~1.1k dps、峰 **2177 dps**、削顶帧 0~4%；|a| 1.0~7.4 g），
随后**放回原位**静置 5 s。

**首尾同位姿自检（"放回原位"由数据确认）**

| 项 | 值 |
|---|---|
| 磁场机体系首尾夹角 | **0.24°** |
| 重力机体系首尾夹角 | **0.34°** |
| **姿态估计首尾差（世界系）** | **0.25°**（含 yaw） |
| 首尾静置 th_mag | 0.23° → 0.21° |
| 首尾静置 th_grav | 1.08° → 1.11°（**恒定偏置，非漂移**） |

⇒ 摇晃 20 s 后回到原位，姿态（含航向）复原到 **0.25°**，**没有累积**。

**全程指标（逐秒）**：`mag_used` 47.3~48.7%（= 新鲜度上限）、`mag_rej` **全 0**、
`nis4` p50 ≤ 0.53、磁残差 rx p50 ≤ 1.54° / p90 ≤ 5.32°、静置 rx 0.05~0.11°、`res` 0.34~0.40°。
**停稳无 snap**：停稳后 1 s Σ|dq_yaw| = **4.87°**、单帧 max **0.13°**（对比 030649：Σ 80°/s、单帧 2.0°）；
末段静置 4.7 s Σ|dq_yaw| 13.5° ≈ 噪声级。

**与 §7.7 那次（030649）的 A/B —— 大修正的成因闭合**

| | 030649（出大修正） | 033915（本次） |
|---|---|---|
| \|ω\| p50 / 峰 | ~2000 / 2763 dps | 0.8~1.1k / **2177** dps |
| 削顶帧占比 | **20~43%/s**（持续 8 s） | **0~4%**（仅两秒） |
| 磁盲区（绕 b̂）残余 | **109°** | **~1°**（末段 th_grav 1.11°，且首尾一致） |
| 停稳后释放 | Σ 80°/s、单帧 2.0°、拉 55° | Σ 4.9°/s、单帧 0.13°、几乎无事 |

⇒ **"停稳后那次大修正" = 角速率超过 ±2000 dps 量程的直接后果**（削顶期间绕 b̂ 的模式无任何观测者），
不是融合逻辑缺陷；而 ±2000 dps 是 ICM-42605 的上限（该档不可再高），融合层无法补救。
只要不长时间超量程，盲区残余就是度级、并会被重力通道以 `TILT_K_MAX=0.010`（≈0.3 s）平滑收回。

**结论**：VER=107 基线**通过**本次验收。可选（非必需）：`V5F_EKF_MAG_VEC_K_MAX` 0.10 → 0.2~0.3，
把机动期可见 2 维的 p90 残差（现 5.3°）再收紧一档。

### 7.9 站点磁要素复核（WMM2025 实算）：常量其实对应**北京**，注释里的"青岛"是错的

用户读数（对准正北，验收流 4 通道 JustFloat 四元数 `q=(w,x,y,z)`）：
`CH0..CH3 = 0.1298, -0.0067, 0.0079, -0.9915`。

**WMM2025 实算**（`tools/calib/wmm_site.py`，pygeomag + 自带 `WMM_2025.COF`，历元 2026.72，alt 0）：

| 点位 | D 磁偏角 | I 磁倾角 | tan I | H (nT) | F (nT) |
|---|---|---|---|---|---|
| **40.00N 116.25E（北京）** | **-7.54°** | **+59.50°** | **1.698** | 30200 级 | — |
| 36.23N 120.44E（青岛，注释里写的） | -7.64° | +54.33° | 1.393 | 30466 | 52247 |

⇒ 固件 `V5F_MAG_DECL_DEG=-7.53` / `V5F_EKF_DIP_TAN=1.70`（I=59.53°）**与北京几乎完全一致（差 0.03°/0.005）**；
而 `v5f_tune.h` 注释里写的来源"@ 36.23N 120.44E"（青岛）**与实际常量不符**——青岛的真实磁倾角是 54.33°。
反查（`--match -7.53 59.53`）在全中国网格上的最优解就是 **40.00N 116.25E**（次优 40.00N 116.00E）。
**§7.3 里"原常量像是取错了纬度（差 ≈4.7°）"的说法因此作废**：常量没错，是注释标签错了。

**旁证（强）**：本项目自己的录像实测磁倾角（标定后磁场与重力夹角 − 90°）为 **59.04 / 59.98 / 59.73 / 57.70°**
—— 与**北京**的 59.50° 吻合（差 ~0.5°），与青岛的 54.33° 差 5°。
这条同时说明：**磁标定 A/C 没有把场方向拧歪**（否则实测倾角会系统性偏掉），
之前"动态倾角攒到 55° / 大修正"的结论不受影响。

**磁北极（dip pole）当前位置**（同模型实算）：**85.40°N 133.50°E**，H=7.2 nT、I=89.993°（历元 2026.72）。

**读数解读**（世界系由 `b0=(ci·sinD, ci·cosD, −tanI·ci)` 与对齐式
`dpsi = atan2(Bn_x, Bn_y) − D` 定出：**+y = 真北、+x = 东、+z = 天**，即姿态是**绝对于真北**的，
不是"开机相对"的）：

- `|q| = 1.000`；水平轴倾角 **1.19°**（器件基本水平）；绕天顶转角 **yaw = 2·atan2(z,w) = −165.08°**。
- 各机体系轴在世界系的罗盘指向：`+x → 255.1°`、`+y → 165.1°`、`−x → 75.1°`、**`−y → 345.1°`**。
- 关键约定：本固件"指真北"不等于"yaw=0"。若机头是 **+x**，指真北应读 **+90°**（q=(0.707,0,0,+0.707)）；
  读到的形状（CH0≈0.13、CH3≈−0.99，即 ~180° 偏航）**只与"指北的那根轴是 −y"自洽**，此时航向误差 **15.1°**。

**结论（合理性）**：数值本身干净（模长 1、水平、无跳变），绝对基准也对（对齐确实减掉了 D）；
但 ~15° 的剩余航向差**不是常量误差**能解释的（D 只差 0.03°、I 与实测吻合），
可疑点依次为：① 测试点的磁干扰（室内钢桌/主机/USB 线，十几度很容易）；
② "真北"参考本身的精度；③ 360° 桌面标定对水平面的约束弱（残余偏航误差）。
④ **机头轴向必须先定死**（+x 还是 −y），否则每次"指北"都会读出 90°/180° 的假误差。

### 7.10 用户确认在**青岛** ⇒ 实测场方向偏陡 5.5°，且误差在**原始层**（§7.3 的 1.70 是拟合到了被拧歪的测量）

**WMM2025 目标值**（36.23N 120.44E，历元 2026.72）：D = **−7.639°**、I = **+54.330°**、tan I = **1.3932**、
H = 30466 nT、F = **52247 nT**（= 52.2 µT），V = F·sin I = 42434 nT。

**实测（两条录像的静置帧：|ω| < 5 dps 且 |a| ≈ 1 g；磁倾角 = ∠(m̂, ĝ) − 90 是姿态无关量）**

| 录像 | 原始 LSB 倾角 p50 | 标定后 A·raw+C 倾角 p50 | 原始 \|B\| p50 | 原始 水平/竖直 | V/H |
|---|---|---|---|---|---|
| 030649 | **59.96°**（p10-90: 59.67~60.33） | 59.77° | 53.2 µT（**+1.8%**） | 27.4 / 45.6 µT | **1.666** |
| 033915 | **59.83°** | 59.05° | 53.2 µT（+1.8%） | 27.3 / 45.6 µT | **1.671** |
| WMM 真值 | **54.330°** | — | 52.2 µT | 30.5 / 42.4 µT | 1.393 |

⇒ 三条硬结论：
1. **场强是对的**（+1.8%），**方向偏陡 +5.5°**（V/H 1.67 对 1.39，水平分量小 ~10%、竖直分量大 ~7%）。
2. 这个偏差**在原始数据里就有**，固件的 A/C 标定只把它从 59.96° 改到 59.77°（−0.2°）——
   **不是标定引入的，也没被标定修掉**。
3. 所以 §7.3 把 `V5F_EKF_DIP_TAN` 从 2.08 改成 **1.70**，等于**把模型拟合到了被拧歪的测量上**：
   系统内部自洽（因此所有内部门控/创新/NIS 指标全绿），但**绝对场方向偏 5.5°**，
   经 tan(I)≈1.70 的耦合放大 ⇒ **航向误差可达 ~9°**（这解释了"指真北读 15°"的大部分）。

**误差可能落在哪（原始层，三选一，需两步分辨）**
- ① 磁↔加速度计**轴向不对齐**（两颗独立芯片，焊装倾斜 ~5°）；
- ② 该测点**环境软磁**（桌面/设备/线缆）在当时把场压扁；而 A/C 是在**同一个**畸变环境里拟合的，
  所以它把畸变"学进去了"，换个点就不对；
- ③ 加速度计方向被标偏 ~5°（但用户指北读数里倾角只差 1.19°，若当时确实水平 ⇒ ③ 不像）。

**修正顺序（顺序不能反）**
1. **先在干净场地（室外/离金属 2 m）做三维翻滚标定**（要覆盖 ±z，桌面 360° 平转定不出竖直方向的
   软磁/标度），然后**先验验收**：倾角必须读 **54.33° ± 0.5°**、\|B\| = **52.2 µT ± 2%**。
   - 若三维标定后仍读 59.8° ⇒ 不是软磁，改查 ①（磁-加速度计轴向不对齐，用仓库里的
     联合拟合/`_misalign` 类工具做 3×3 对齐矩阵）。
2. 标定过关**之后**再改常量：`V5F_EKF_DIP_TAN = 1.393`、`V5F_MAG_DECL_DEG = -7.64`
   （D 现在 −7.53 只差 0.11°，可不动）。
   - **不能只改常量**：模型一改，磁创新里会出现一个永远消不掉的 ~5° 分量，滤波器会一直跟它较劲
     （表现为倾角偏置 + 被 K 上限拖住的慢漂）。
3. 重跑验收（含 §7.8 的"回原位"口径）。

**方法论补充（重要）**：本项目此前所有验收指标——世界系磁场方向残差、磁创新 `th`、`nis4`、`dq_yaw`、
回原位复现——都是**内部一致性**指标：一个**恒定的测量畸变/整体旋转**会被姿态原样吸收，
**它们天然看不见**。所以"全绿"并不等于绝对正确。**必须补一条绝对项**：
与 WMM2025 比 **D / I / |B|**（不需要指北、不需要转台，静止 5 分钟即可），
本节的表格就是它的实现（`tools/calib/wmm_site.py` 给目标值）。

### 7.11 低干扰点位录像（`imu_20260921_040450.bin`）：台面标定被环境污染**已证实**，但这条数据还出不了新标定

`fw_tag 7119367` → **VER=108、162 通道调试帧**（说明该 build 里 `V5F_CDC_QUAT_ONLY=0u`）、30106 帧 / ~89 s、
校验和 100%。新增常驻绝对项工具 `tools/calib/mag_site_check.py`（不需要指北/转台）。

**绝对项（与 WMM2025 36.23N 120.44E：D=−7.639°, I=+54.330°, F=52.2 µT 比）**

| 录像 | 点位 | 原始倾角 p50（p10~p90） | ΔI | 原始 \|B\| | Δ\|B\| | 原始 V/H |
|---|---|---|---|---|---|---|
| 040450 | **低干扰** | **47.61°**（46.48~69.51） | **−6.72°** | 51.9 µT | **−0.6%** | 1.154 |
| 030649 | 台面 | 59.96°（59.67~60.33） | +5.63° | 53.2 µT | +1.8% | 1.666 |
| WMM 真值 | — | 54.330° | — | 52.2 µT | — | 1.393 |

三条结论：
1. **两个点位场方向差 12.3°，场强几乎不变（±2%）** ⇒ 台面存在**强方向性畸变**（钢桌/设备软磁），
   而现用 A/C（VER=102）就是**在台面那次录像上拟合的** ⇒ **标定被环境"学进去了"**。
2. **直接证据**：拿现用 A/C 在这条干净数据上算，方向残差 **p50 = 17.0°**（在台面那条上是 0.48~0.95°）。
   即同一个标定换个环境就差 17°——这不是精度问题，是环境特化。
3. 新点位虽"低干扰"，原始测量仍然**不是刚体场**：视倾角随姿态从 46.5° 变到 69.5°（p10~p90），
   所以**必须重新标定**，不能只改 dip 常量。

**但这条录像做不出有效标定**（`mag360_cal.py` 判据）：

| 判据 | 本次 | 要求 |
|---|---|---|
| 有效样本 | **2979** / 30106（9.9%） | ≥ 3000 |
| 姿态激励 | roll **356°** / pitch **53°** / yaw 79° | roll ±180 **且** pitch ±90（俯仰/翻转必须补） |
| 零空间维数 | **6** | 1（纯标度自由度） |
| 半分一致性 ‖A1−A2‖/‖A‖ | **1.339** | ≪ 0.1 |
| 未激励方向能量 | C(偏置) 2.97、w0(世界场) 2.03 | —— 即"竖直/出平面"方向没被激励 |

⇒ 工具自己给的结论："平面 360 转再多圈也补不上，必须加俯仰/翻滚/翻转。"

**要补的录像（干净点位，全球面）**
- 覆盖：**roll ±180°、pitch ±90°、3~4 个方位角**；推荐**12~16 个不同姿态，每个停 3~5 s**（静止样本才进选样），
  中间缓慢过渡；总时长 **90~120 s**。
- 选样门槛（工具的）：`|ω| < 50 dps` 且 `||a|−1| < 0.02 g` —— 所以"多停、慢转"比"快转多圈"有效得多。
- 位置：离金属 ≥2 m、非金属台面；同一位置最好**重复录一条**做交叉验证（半分一致性才有意义）。
- 拟合后**绝对项验收**：倾角必须 **54.33° ± 0.5°**、`|B|` = **52.2 µT ± 2%**（`mag_site_check.py` 一条命令给出）。
  若新 A/C 仍把倾角拉不回 54.33°，剩下的就是**磁↔加速度计轴向不对齐**（两颗独立芯片），
  那一步要用 3×3 对齐矩阵而不是软磁椭球。

**工具修复**：`mag360_cal.py` / `mag360_refcheck.py` / `mag_effect.py` 现在强制 UTF-8 输出
（GBK 控制台编不出 `m̂` 的组合符，会让拟合跑到一半崩掉——本次就是）。

**方法论复核**：本节再次说明 §7.10 的判断——此前所有"全绿"验收（世界系磁场残差、磁创新、nis4、dq、
回原位 0.25°）都是**内部一致性**指标，一个环境特化的标定 + 一个跟着它调出来的 dip 常量，
在这套指标下完全可以"全绿"。**绝对项（WMM）是唯一能揭穿它的**。

### 7.12 更正 §7.11：数据本身是**完整全球面**（是我的选样门槛错的）；改用"椭球 + 重力对齐"标定

**1) 更正**：§7.11 说"这条录像姿态覆盖不足"是**错的**——那是我用 `mag360_cal.py --amag 0.02`
（`||a|−1| < 2%`）筛样本造成的：**姿态辅助标定用旧链 att.q 当参考，根本不需要重力有效**，
重力门槛只对"倾角/dip 检查"有意义。放宽到 `--wmax 2000 --amag 1.0` 后：

| 选样 | 通过 | roll | pitch | yaw 累计 |
|---|---|---|---|---|
| `--amag 0.02 --wmax 50`（错） | 2979（9.9%） | 356° | **53°** | 79° |
| `--amag 1.0 --wmax 2000`（对） | 14965（49.7%） | 360° | **177°** | **1015°** |

⇒ 用户动作**是**完整的全球面翻滚。`mag360_cal.py` 的 `--amag` 默认已放宽，并加注"不许用重力门槛判覆盖"。

**2) 但姿态辅助拟合的天花板是"参考姿态"本身**：放宽后 [现用 A/C] 方向残差 p50 **4.12°** / p90 9.35°，
[本次重拟合] p50 **4.02°** —— 换标定几乎不动。原因：旧链 att.q 是**无磁**陀螺链，
本录像 89 s 内 yaw 累计 1015°、陀螺零偏 0.36 dps ⇒ **~30° 偏航漂移**；
而现用 A/C 在它拟合的台面数据上是 0.48°，在这条干净数据上 4.12°（差 8 倍 ⇒ 台面特化已被证实）。

**3) 新路径 `tools/calib/mag_ell_cal.py`：椭球 + 重力对齐（姿态自由）**
- ① 椭球：`|A·raw + C| = const`，线性最小二乘，10 参数 → 定出软磁/标度/硬铁（= A^T A 与 C），
  **完全不使用姿态参考**；本次各向异性 19.1%。
- ② 取向：椭球只给到 `A0 = chol(A^T A)`，还差一个 SO(3) 自由度 U（A = U·A0）。
  用**重力**钉住：要求标定后磁场与实测重力夹角 = 90° + WMM 倾角 **I = 54.330°**（准静止帧 6496 个，
  多姿态 ⇒ 超定）。只用重力，不用 WMM 的方位/偏航。
- 结果（准静止帧上的倾角统计）：

| 标定 | p10 / p50 / p90 | ΔI(p50) | 展宽 p10~p90 |
|---|---|---|---|
| 原始 LSB | 46.55 / 51.37 / 70.41 | −2.95° | **23.86°** |
| 现用 A/C（VER=102） | 48.44 / 49.88 / 60.78 | −4.45° | **12.34°** |
| **椭球 + 重力对齐** | 52.44 / 54.16 / 56.04 | **−0.17°** | **3.59°** |

原始 \|B\| p50 = 51.6 µT（WMM 52.2，**−1.2%**）；重力对齐多起点解残差 RMS **3.19°**。

**4) 两点必须说清楚**
- **剩余 3.59° 展宽 + 3.19° 对齐残差是"姿态相关"残余**，最可能来自**加速度计自身标定**
  （倾角 = 磁场与*实测重力*的夹角，加速度计方向错多少，倾角就错多少）。
  ⇒ 下一步应先做加速度计六面/多姿态标定，再重跑本工具。
- **A 的方位角（绕竖直）自由度在固件里由开机对齐吸收**：align 把世界系定成"实测磁场方位 = 声明磁偏角 D"
  （`dpsi = atan2(Bn_x, Bn_y) − D`），所以 A 的方位规范不影响 EKF 的绝对航向；
  真正影响手感的是**磁↔加速度计相对取向**，正是重力对齐钉住的那部分。
- **配套必改**：`V5F_EKF_DIP_TAN` 1.70 → **tan(54.330°) = 1.3932**。
  换了 A/C 却留着 1.70，模型场比真实场陡 5.2°，滤波器会一直跟它较劲（倾角偏置 + 慢漂）。

**5) 本次得到的候选 A/C**（标度沿用固件 median|y| = 1 约定）：
```
A 行0 { +2.87508917e-04f, +7.28617565e-04f, +1.01406737e-03f }
A 行1 { -7.09199398e-04f, +8.22262668e-04f, -3.47199115e-04f }
A 行2 { -8.03753696e-04f, -4.44306852e-04f, +5.65119343e-04f }
C    { +5.99541656e-02f, +5.96288002e-01f, -6.58694019e-01f }
```
**未写回固件**：等加速度计标定补上（残余 3.6° 主要来自它），或用户指定先上这一版。

### 7.13 "小旋转也有残差、静止时被修正"的量化：是**模型-测量不一致**，不是门控也不是 K 上限

用户在 VER=108 上观察：即使小旋转也出现明显残差，静止时被修正。按 |ω| 分箱量（`imu_20260921_040450.bin`，低干扰点位）：

| \|ω\| dps | 帧数 | `mag_used`% | rx p50（yaw 轴 e1） | ry p50（tilt 轴 e2） | th p50 | nis4 p50 | `mag_rs` |
|---|---|---|---|---|---|---|---|
| 0~5（静止） | 4802 | 47.9 | 0.01° | **10.59°** | **10.66°** | 0.07 | **2500** |
| 5~20 | 2823 | 48.0 | 0.00° | 0.01° | 0.30° | 0.02 | 12 |
| 20~60 | 4659 | 47.7 | 0.02° | 0.08° | 0.43° | 0.02 | 169 |
| 60~150 | 5411 | 48.1 | −0.01° | 0.19° | 0.69° | 0.03 | 2276 |
| 150~400 | 11826 | 47.8 | −0.07° | 0.05° | 0.77° | 0.04 | 2500 |
| 400~1000 | 585 | 48.4 | −0.15° | −0.27° | 0.77° | 0.04 | 2500 |

三条硬结论：
1. **不是转速门**：`gate->ekf_mag_yaw` 的判据只有 `s_mn_ok && V5F_EKF_YAW_OBS_EN`（代码 proc_ekf.c:1726），
   没有 |ω| 条件；实测 `mag_used` 在所有转速下都是 **48%**（= 新鲜度上限）。
2. **不是 K 上限**：yaw 轴创新 rx p50 = 0.00~0.15°（各转速），`|dq|` 中位数 **0.000** —— 创新本来就是 0，
   没有东西可修。（K 上限的作用在**剧烈段**才显出来：那条录像 rx p50 4.2°、dq/rx = 0.197 = K_MAX/cos I。）
3. **真正的量是静止时的 tilt 轴残差 10.59°**，而 `mag_rs` = **2500**（`conf` 落到下限 0.02 ⇒ σ_tilt = 45°）
   ⇒ **滤波器明知这 10.6° 却不修**：因为实测倾角（49.9°，见 §7.11）与模型倾角（59.53°）差 ~11°，
   `conf = 1/(1+(ddip/1°)²)` 判定"磁场不可信"，把 tilt 通道权重压到 1/50。
   转动起来后加速度被污染 → `dip_m` 变噪声 → conf/rs 随机跳动（12~2500）→ 残差看起来"随姿态出现/消失"。

⇒ 所以"小旋转也有残差、停住就被修正"= **模型与测量的不一致（~10.6°）在不同姿态下的投影变化**，
根因仍是 §7.10/§7.12 那两条：**台面污染的 A/C** + **dip 常量偏陡 5.2°**。
修好 A/C 与 DIP_TAN 之后 `conf` 会回到 ~1，tilt 通道恢复权重，这个静止残差才会消失。
**这个阶段抬 K_MAX 毫无意义**（创新为 0）。

**加速度计：本录像标不出来**
- 静置帧（|ω|<5 dps，4751 帧）的加速度方向**只覆盖一个平面**：椭球拟合秩亏，
  轴比 0.000 / 0.587 / 1（退化）。含运动的帧不能用（有真实线加速度，|a|≠1 g）。
- 现有标度的**幅度**没问题：原始 |a| 的 p10~p90 展宽只有 **1.6%**。
  §7.12 剩下的 3.6° 倾角展宽更像**方向**误差（轴向不对齐）或**翻滚空间内的场梯度**，不是幅度。
- 要标加速度计 + 把磁的 3.6° 展宽压下去，只需按协议补一条：**同一位置、12~16 个真正分散的姿态，
  每个停 3~5 s**（roll ±180、pitch ±90、3~4 个方位角），90~120 s。
  拿到后链路是：加速度计椭球（|a|=1 g 的静置帧）→ 磁椭球 → 重力对齐 → 绝对项验收。

**工具修正**
- `mag_ell_cal.ellipsoid_fit()` 现在**先中心化+归一化**再拟合：加速度计原始 LSB 偏置大、球心远离原点，
  直接用 `u^T M u + b·u = 1` 参数化会病态到 NaN（本次就踩到）。
- `mag360_cal.py` 的 `--amag` 默认放宽（§7.12）：**不许用重力门槛判"覆盖是否足够"**。
